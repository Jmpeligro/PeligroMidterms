1) Circles with many circles

@Override
protected void onDraw(Canvas canvas) {
    super.onDraw(canvas);
    Paint paint = new Paint();
    
    int[] colors = {Color.RED, Color.GREEN, Color.YELLOW, Color.BLUE, Color.MAGENTA};
    int radius = 50;
    for (int color : colors) {
        paint.setColor(color);
        canvas.drawCircle(200, 200, radius, paint);
        radius += 30;
    }
}

2) Rectangle with section text 

 @Override
protected void onDraw(Canvas canvas) {
    super.onDraw(canvas);
    Paint paint = new Paint();

    // Draw magenta rectangle
    paint.setColor(Color.MAGENTA);
    RectF rect = new RectF(50, 100, 450, 200);  // position and size
    canvas.drawRect(rect, paint);

    // Draw text
    paint.setColor(Color.WHITE); // white text for visibility
    paint.setTextSize(40);
    paint.setTextAlign(Paint.Align.CENTER);
    
    float x = rect.centerX();
    float y = rect.centerY() - ((paint.descent() + paint.ascent()) / 2);  // center vertically
    canvas.drawText("YOUR SECTION HERE", x, y, paint);
}

3) Rectangle with cross

@Override
protected void onDraw(Canvas canvas) {
    super.onDraw(canvas);
    Paint paint = new Paint();
    paint.setStrokeWidth(5);
    
    paint.setColor(Color.GREEN);
    canvas.drawLine(0, 0, getWidth(), getHeight(), paint);
    
    paint.setColor(Color.BLUE);
    canvas.drawLine(0, getHeight(), getWidth(), 0, paint);
}

4) Pacman

@Override
protected void onDraw(Canvas canvas) {
    super.onDraw(canvas);
    Paint paint = new Paint();
    paint.setStyle(Paint.Style.FILL);
    
    paint.setColor(Color.MAGENTA);
    canvas.drawArc(new RectF(100, 100, 300, 300), 45, 270, true, paint);
    
    paint.setColor(Color.YELLOW);
    canvas.drawCircle(180, 150, 10, paint); // eye
}
